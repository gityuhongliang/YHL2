//store 的计算属性
export default {
    detail(state) {
        return state.item
    },
    // details(state){
        // console.log(state.item.detail);
        // console.log(state.item.detail.images);

    // }
}